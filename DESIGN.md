# 铸壳工具链（ForgeShell）· `s2a` —— 设计文档

本文说明**程序真正的做法**：每个阶段的输入输出、**密钥怎么来**、每一段密文的**字节布局**、
以及加密/加固的**全部参数**与**局限**。要"怎么用"看 [`README.md`](README.md)；
要用户手册跑 `s2a --help-full`。

**目录**：1 数据流 · 2 模板与拼装 · 3 三种产物布局 · 4 KDF v2 · 5 池加密 · 6 完整性自检 ·
7 口令锁与来源 · 8 整文件加密 · 9 帮助文本加密 · 10 反调试 · 11 内嵌工具 · 12 外壳 ·
13 威胁模型与局限 · 14 已知缺陷 · 15 踩坑清单 · 16 许可证

---

## 1. 数据流总览

```
脚本 ──[src/common/parse.c]──► AST ──[compiler/codegen.c]──► AArch64 机器码 + 只读/可写数据
                                        │
                                        ├──[compiler/image.c]──► 运行时映像（字符串池、AST 镜像、常量）
                                        │                        └─[compiler/protect.c]─► 池加密 + 摘要 + 校验料
                                        └──[compiler/elf.c]────► 与运行时模板拼装 → 产物 ELF
                                                                  （追加 PT_LOAD、patch rc_user_main）
运行时模板（build/template.elf，静态 musl，自带 rc_* 符号表）在编译期被内嵌进 s2a。
```

三条**运行时**路径：

1. **编译产物**：语句已是机器码，启动即执行，无解析开销。
2. **解释器路径**：`$( )`、`eval`、`source/.` 的动态片段 —— 运行期用**同一份 `parse.c`**
   （编进运行时映像）解析后解释执行，语法与编译期完全一致。
3. **模拟器 / qemu**：`-r` 用内置模拟器解释执行产物机器码；缺指令或访存故障时自动回退到
   内嵌 qemu（`--no-fallback` / `--emu-own` 可关）。

---

## 2. 运行时模板与产物拼装

* 模板 = 静态 musl 程序：`_start → __libc_start_main → main() → rc_user_main()`。
* 编译期解析模板符号表 → 拿到 `rc_*` 绝对地址 → 生成代码作为**新 PT_LOAD** 追加到文件末尾
  （`#1 RX`：机器码 + 只读数据；`#2 RW`：运行期可写数据）→ patch `rc_user_main` 指针。
* **两条硬规则**（踩坑得来）：
  1. 程序头表必须落在第一个 LOAD 段的文件范围内 —— 加载器用
     `AT_PHDR = 段基址 + (e_phoff − 段文件偏移)` 定位，否则启动即崩。
  2. 可执行段不得携带 bss：`filesz` 与 `memsz` 都要页对齐。

---

## 3. 三种产物的字节布局

### 3.1 普通编译产物（约 451 KB，**不带任何工具**）

```
[ 模板 ELF（静态 musl，代码段基址 0x100000） ]
[ 追加 PT_LOAD #1（RX）：生成代码 + 只读数据（字符串池密文、AST 镜像、常量） ]
[ 追加 PT_LOAD #2（RW）：运行期可写数据 ]
```
工具（qemu/ash）**不在产物里**；它们只存在于 `s2a` 自己身上（见 §11）。

### 3.2 `-easy` 产物（约 183 KB）

```
[ 外壳 launcher.elf（88,184 B） ] [ ash blob（94,816 B，UPX） ] [ 脚本正文 ] [ 128 B 索引 ]
```
外壳**不加密**脚本正文（有意为之，见 §12）；需要加密就整份交给 `--only-enc`。

### 3.3 `--only-enc` 产物

```
[ 外壳 launcher.elf（88,184 B） ] [ AES-256-CTR 密文（长度 = 原文件长度） ] [ 128 B 索引 ]
```

---

## 4. KDF v2（密钥派生）—— 本轮升级重点

旧版是"一次 SHA-512 / 一次 SHA-256"，现在**两条路径统一走 PBKDF2-HMAC-SHA256**：

| 路径 | 派生式 | 盐 | 迭代次数 |
|---|---|---|---|
| 口令 → 池密钥（`-P`/`-F`） | `PBKDF2-HMAC-SHA256(口令, 盐 = 产物池前 16 字节)` | **每产物不同** | `S2A_KDF_ITERS = 5000` |
| 口令 → 整文件密钥（`--only-enc`） | `PBKDF2-HMAC-SHA256(口令, 盐 = 尾部随机 salt[16])` | **每文件随机** | 同上（尾部记录） |
| seed → 池密钥 | `kdf_strong(seed[0:32], "protect-pool", 盐=seed[32:48])` | 域串 + seed 后半 | 同上 |
| seed → 校验密钥 | `kdf_strong(seed[0:32], "protect-check", 盐=seed[32:48])` | 同上 | 同上 |
| seed → 整文件密钥（无口令） | `kdf_strong(seed[0:32], "onlyenc-seed", 盐=nonce[16])` | 域串 + nonce | 同上 |

⚠️ 上表是**两条独立的链**：字符串池加密（编译产物内部，防 `strings`）与整文件加密
（`--only-enc`，外壳 + 密文）各有自己的盐来源与尾部字段，不要当成同一种 KDF 读。

* 实现：`src/common/crypto.c` 的 `hmac_sha256()` / `pbkdf2_sha256()` / `kdf_strong()`；
  声明在 `src/common/crypto.h`，迭代数宏 `S2A_KDF_ITERS` 也在那里。
* **域分离**：`salt' = purpose || salt`，避免同一秘密在不同用途下派生出相同密钥。
* **次数是实测校准**（已核对）：微基准 10 次 × 1000 次迭代共 35 ms → **1000 次 ≈ 3.5 ms**，
  故 5000 次 ≈ **17 ms**；实测**带防护产物的整段启动 ≈ 33 ms**（含池解密 + 自检 + 反调试探测）。
* ⚠️ **两侧必须同值**：编译器 `protect.c` 与运行期镜像 `rt_protect.c` 的派生参数必须完全一致，
  否则池解出来是乱码（现象：产物把乱码当命令名，报"未找到该命令"）。
  `--only-enc` 的外壳与编译器同理。改迭代数请一起重编。
* 想更强：调大 `S2A_KDF_ITERS` 重编即可；想更快：调小（安全性线性下降）。

---

## 5. 加密一：字符串池（`-P` / `-F`）

| 项 | 值 |
|---|---|
| 算法 | AES-256-CTR |
| 密钥 | `kdf_strong(seed[0:32], "protect-pool", seed[32:48])`（KDF v2） |
| 计数器初值 | `nonce = seed[32:48]` |
| 随机源 | `/dev/random`，每次构建的 **64 字节 seed**（**无硬编码密钥**） |
| 加密对象 | 字符串池（脚本字面量、变量名、诊断文案等） |
| 解密时机 | 产物启动时在内存中还原后使用 |

> ⚑ **口令是"加密 ↔ 混淆"的分界线**：不给口令时，池密钥的原料（seed）就存在产物里，
> 拿到文件的人可以自行解开 —— 这属于**混淆**（防 `strings`、防随手看、防特征扫描）；
> 给了口令才是**真加密**（密钥由 `PBKDF2(口令, 每产物盐)` 派生，产物里没有口令）。

**实测（脚本含 `SECRET_NAME` 字面量）**：

| 开关 | 产物里能否搜到明文 | 能否运行 |
|---|---|---|
| 无 | 能（1 处） | 能 |
| `-P` | **否（0 处）** | **能（无需口令）** |
| `-F` | **否（0 处）** | **能（无需口令）** |
| `-P --password*` | 否 | 需对口令，否则退出码 70 |
| `-easy` | 能（脚本正文原样） | 能 |
| `-easy` + `--only-enc` | **否** | 能 |

---

## 6. 加密二：完整性自检（`-P` / `-F`）

* `code_sha512 = SHA-512(机器码段)`、`pool_sha512 = SHA-512(池)`，随产物保存。
* 另有一份**每次构建随机的校验料 V**（16 字节），只以 **AES-CTR 密文**形式出现在产物里；
  其密钥 `K = kdf_strong(seed[0:32], "protect-check", seed[32:48])`。
* 启动流程：还原 V → 解开校验料 → 与摘要比对；任一步不符即判定被改动。
  改动代码段或池（哪怕一个字节）都过不了。
* 自检结果计入反调试的"检测层"（§10），按 `--anti-action` 处置。

---

## 7. 加密三：口令锁与四种口令来源

口令**不存储**：`PBKDF2-HMAC-SHA256(口令, 盐 = 该产物池密文的前 16 字节, 5000 次)`
派生出 64 字节密钥材料（每产物不同盐，见 §4/§13）；
口令不对 → 池解出垃圾 → 自检失败 → 退出码 **70**。

| 来源 | 写法 | 说明 |
|---|---|---|
| 直接给值 | `--password '<口令>'` / `--password=<口令>` | 同以前 |
| 环境变量 | `--password-env <变量名>` | 变量不存在或为空 → 退出码 70 |
| 文件 | `--password-file <文件>` | **内容原样**（等价 `cat`）；`echo` 会多一个换行，建议 `printf '%s'` |
| 环境兜底 | `S2A_PASSWORD=<口令>` | 上面都没给时自动取用 |

* **编译期与运行期都支持**这四种。产物运行时这些参数会被取走，**其余参数照旧原样分发给脚本**。
* 读文件用**分块读到尾**实现（不用 `fseek/ftell`），所以管道式路径 `<(printf 'pw')` 也能用。
* 口令只经命令行/环境/文件传入，不写进产物、不写进记忆、不写进日志。

---

## 8. 加密四：整文件加密（`--only-enc`）

把**任意 ELF**（本工具链产物、系统 ash、你自己的程序）变成"外壳 + 密文"：

```
偏移            长度      含义
0               88,184    launcher.elf（外壳，两模式共用）
88,184          原文件长   AES-256-CTR 密文（内容 = 原 ELF 全部字节）
末尾 128        128       索引（下表）
```

尾部 128 字节索引：

| 偏移 | 长度 | 含义 |
|---|---|---|
| 0 | 8 | `"S2ALAU02"`（v2；`-easy` 用 `"S2ALAU01"`） |
| 8 | 8 | mode：`1` = enc（`0` = easy） |
| 16 | 8 | off1 = 密文偏移（u64 小端） |
| 24 | 8 | len1 = 密文长度 |
| 32 | 8 | off2（easy 用；enc 未用） |
| 40 | 8 | len2（同上） |
| 48 | 32 | seed |
| 80 | 16 | nonce |
| 96 | 16 | salt（有口令时 PBKDF2 的盐） |
| 112 | 4 | pw_flag（`1` = 带运行期口令门禁） |
| 116 | 4 | iters（PBKDF2 迭代次数） |
| 120 | 8 | `"S2AEND02"`（旧文件 `"S2AEND01"`） |

旧 v1 尾部（`S2ALAU01`/`S2AEND01`）仍可运行：enc 的密钥按旧方案
`SHA-256(seed[0:32] ^ SHA-512(口令))` 派生，仅为兼容早期产物而保留读路径。

* 密钥派生（**v2，魔数 `S2ALAU02`**）：
  * **给了口令** → `key = PBKDF2-HMAC-SHA256(口令, 随机盐 salt[16], 5000)`；
    运行期**必须**再给同一口令（`--password` / `--password-env` / `--password-file` / `S2A_PASSWORD`），
    否则退出码 70；解出来先验 `\x7fELF` 魔数，口令错就报"口令错误或文件被改动"（同样 70）。
    这些口令参数会被外壳取走，**不会**传给内部程序。
  * 没给口令 → `key = kdf_strong(seed[0:32], "onlyenc-seed", 盐=nonce[16])`，运行期免口令。
  * 旧文件（魔数 `S2ALAU01`）外壳仍能解（沿用旧方案），无需重做。
* **嵌套（可多层）**：`--only-enc` 的输出可以再被 `--only-enc`。外层解出口令后会把它
  **导出成环境变量 `S2A_PASSWORD`** 再执行内层，并且**只吃第一个**口令参数（后续的留给内层），
  所以内层是口令锁产物/口令加密产物时也能解开。
  例：`./两层.enc --password 外层 --password 内层`；同口令时给一次即可。
  （修之前：外层把 `--password` 全吃走，内层永远拿不到口令 → 表现为"永远无法解密"。）
* ⚑ 同样地：**不给口令 = 混淆**（seed/nonce 就在尾部，谁拿到谁能解）；**给口令 = 真加密**
  （运行期门禁 + 密钥不在文件里）。要"别人拿到也解不开"，必须加 `--password*`。
* 运行时：外壳读自身 `/proc/self/exe` 尾部 → 校验魔数 → 解密到**匿名内存页**
  （`memfd_create` + `MFD_CLOEXEC`）→ `fexecve`，**argv 原样透传**；
  内核对 memfd 执行有限制时兜底落盘 → `execv` → 退出前 `unlink`。
* 实测：加密后的 `-easy` 产物照常工作；`grep` 明文命中 0。

---

## 9. 加密五：帮助文本（默认开启）

| 项 | 值 |
|---|---|
| 源文件 | `assets/help-short.txt`、`assets/help.txt`、`assets/help-full.txt`、`assets/help-dev.txt`（仓库里可读可编辑） |
| 加密工具 | `tools/help-enc.c`（Makefile 调用：`build/help-enc 输入 输出 标签`） |
| 算法 | AES-256-CTR（复用 `src/common/crypto.c`） |
| 密钥 | `key = SHA-256("ForgeShell help text v1")` |
| 每档 nonce | `SHA-256("ForgeShell help text v1" + 标签)[0:16]`，标签 = `short` / `detail` / `full` / `dev` |
| 嵌入方式 | 密文 `ld -r -b binary` → 符号 `_binary_build_help*_enc_*`（四个对象：help_short / help / help_full / help_dev） |
| 运行时 | `print_help()` 解密到内存再 `fwrite`；按指针比对准确定位是哪一档以取对应 nonce（档位 0/1/2/3，标签与 Makefile 的 `help-enc` 参数一一对应，写错就是乱码） |

改文案流程：编辑 `assets/*.txt` → `make` → 验证 `strings -a bin/s2a | grep -c 铸壳工具链` 为 **0**。

**踩坑**：初版把 32 字节 SHA-256 结果直接写进 `nonce[16]`，栈溢出踩坏相邻 `key` → 解出乱码。
修法：先写 32 字节缓冲再 `memcpy` 取前 16。

> 这一步与 §5/§8 不同：密钥是**固定字符串派生**（非随机种子），属**混淆级** ——
> 目的是"二进制里搜不到明文"，不是密码学保密。
> 有意保留明文的：`-license` 的 MIT 正文、`Author: deepseek v4 flash & @Chen9183 (github)`、
> 版本号，以及运行期错误提示（runtime 串）。

---

## 10. 反调试（`-P` / `-F`）

| 检测层 | 依据 |
|---|---|
| ptrace | `/proc/self/status` 的 `TracerPid`；`S2A_ANTIDEBUG_AGGRESSIVE=1` 时额外主动探测 |
| maps | `/proc/self/maps` 里的调试器/注入框架特征 |
| 父进程 | 父进程名 |
| 注入 | 环境预载：`LD_PRELOAD` / `LD_AUDIT` / `S2A_DEBUG_TRACE` 等 |
| 断点/自校验 | 代码段断点扫描 + 摘要比对（§6） |
| 时序 | 关键路径耗时异常 |
| /proc/stat | 特征串 |

动作策略（`--anti-action`）：`warn`（默认，一行警告继续）/ `report`（打全表继续）/
`silent` / `fake`（假装正常）/ `delay`（睡 2 秒再继续）/ `stop`。

### 10.1 为什么不会自锁（硬性设计约束）

早期在**主进程**里 `ptrace(PTRACE_TRACEME)`：等于把父进程（shell、终端、任何已有 tracer）
登记成 tracer，而它不会处理 ptrace 事件 → 本进程永远停在 `ptrace_stop`
（现象：启动后 `[1]+ Stopped`、脚本只跑到一半）。现在：

1. **主进程绝不 TRACEME**；
2. 主动探测只在 `S2A_ANTIDEBUG_AGGRESSIVE=1` 时做，且在 **fork 出的子进程**里做
   （父进程负责 `PTRACE_CONT`，子进程被停住不影响本体）；
3. `S2A_NO_ANTIDEBUG=1` 在**探测之前**返回 —— 真正的逃生门（不是"只关动作"）；
4. 安卓终端/root 环境 `TracerPid≠0` 属常态误报，默认只 warn 不影响运行。

---

## 11. 内嵌工具（在 s2a 里，**不在产物里**）

| 工具 | 大小 | 来源 |
|---|---|---|
| qemu-aarch64 静态版 | 1,893,000 B | 官方静态构建，UPX -9 |
| dash 0.5.12（真 ash） | 94,816 B | 源码自编：`musl-gcc -Os` 静态 → `strip`（198,320 B）→ `upx -9` |

* 以 blob 形式链在 **s2a 自己**身上（`ld -r -b binary`）；产物一律不带（普通产物约 451 KB）。
* 用法：`s2a ash -c '…'`、`s2a ash 文件 参数…`、`s2a qemu …`；
  `s2a -r` 遇未实现指令时也用内嵌 qemu 兜底。
* 退出码分开：**解不出工具**（没有可写目录）→ **73**；解出来了但 `execv` 失败 → **126**；
  命令本身的"未找到"才是 **127**（三者不再挤在 127 上）。
* **解出策略**：`$S2A_TOOL_DIR` → `$TMPDIR` → `/data/local/tmp` → `/tmp`；
  文件名按 blob 长度做缓存键（`s2a-ash-94816`）；先落 `目标名.tmp<pid>` 再 `rename`（原子），
  避免半个文件被当成缓存。UPX 件自解压，直接 `execv` 即可。
* dash 覆盖：POSIX 完整 + `[[ ]]` + 算术赋值；不支持 `${v:off:len}`、`${v^^}`、`${v//a/b}`、
  `source`、`{1..3}`、`echo -e`（正是用 `-easy` 时要注意的点）。

---

## 12. 外壳（launcher）设计

`src/launcher/launcher.c`，88,184 字节，尾部 `mode` 字段决定行为：

* **mode 0（easy）**：解出 ash 与脚本正文到临时目录（缓存 + 原子写）→
  `execv(ash, {ash, 脚本, 用户参数…})`。
* **mode 1（enc）**：解密到匿名内存页 → `fexecve`（argv 透传）；受限时兜底落盘再 `execv`。

两种模式都先校验尾部魔数，不匹配报"产物尾部索引损坏"（退出码 126）。

---

## 13. 威胁模型与局限（如实说明）

* **先分清两件事**：不带口令的 `-P`/`-F`/`--only-enc` 是**混淆**（自包含、可自解，只提高门槛）；
  只有**口令模式**才是真加密。下面的"局限"都建立在"攻击者已拿到文件"的前提上。
* **静态时**：脚本字面量（`-P`/`-F`）、整个产物（`--only-enc`）、帮助文本（默认）都不是明文。
* **运行期**：一切都要在内存里还原才能执行 —— 有权限的调试者/root 仍可 dump 内存、`ptrace`、
  或用模拟器观察系统调用。加密提高的是**逆向成本**，不是"不可解"。
* **机器码本身**：脚本**逻辑**被编译成机器码，可反汇编/反编译（字面量被 §5 藏起来）；
  `--only-enc` 只保证"文件在磁盘上不是明文"。
* **UPX 不是加密**：内嵌 qemu/ash 用 UPX 压缩（体积），可原样解出。
* **帮助文本加密**（§9）是固定密钥的混淆级，不是保密级。
* **`--only-enc` 口令已实现为运行期门禁**（v2）：加了口令就必须在运行期给同一口令才能解密，
  否则退出码 70（详见 §8）。旧 v1 文件仍可解。
* `-P`/`-F` 口令锁的 KDF **已加每产物随机盐**：盐 = 该产物池密文的前 16 字节
  （公开、每次构建不同；编译器取加密后的 `c->pool`，运行时取 `img->pool`，两侧必须同段字节）。
  作用：同一口令在不同产物上派生出不同密钥，攻击者无法把一次预计算复用到多份产物。
  （注：无口令时 seed 本身就是 256 位随机数，额外加盐没有收益，故那条路径不加。）

---

## 14. 已知缺陷与差异

* 内置模拟器未实现 **SIMD 单车道存储** `st1 {v?.b|h|s|d}[i], [xn]`（极罕见；默认自动回退 qemu，
  `--emu-own` 时报 132）。同族单车道装载已支持。
* 模拟器覆盖通用寄存器指令集 + 常用 NEON/FP 子集；SVE/SME、CRC32、加密扩展与部分罕见 SIMD
  形式未实现（同样触发回退）。382 条批量对拍中 381 条与真机一致。
* 模拟器原子独占访存是单线程语义（只保证"同地址 + 同尺寸"的独占存储成功）。
* qemu 回退是"从头重跑"，且不走模拟器的覆盖层虚拟文件系统。
* 语言侧：数组/关联数组、`shopt`、`{1..3}` 不支持；`$-` 只列真正支持的选项；
  `${#*}`/`${#@}` 按 POSIX/dash 口径；不做作业控制（`set -m`）。

---

## 15. 开发笔记：踩过的坑（都已修）

1. 程序头表必须在第一个 LOAD 段内（`AT_PHDR` 推导），否则加载即崩。
2. 可执行段不能带 bss；`filesz`/`memsz` 页对齐。
3. 函数实参 argv 必须 NULL 结尾：内建拿到的 argv 没结尾，直接给 `execv` 会越界多读一个参数
   （`command echo A B` 多打一个 `A`）。
4. `$1` 这类位置参数不在变量表里，要单独取；`${#*}`/`${#@}` 要按 `$*` 拼接长度算。
5. 引号内的元字符与**引号内的展开结果**都不参与路径名展开（否则 `"$v"` 会 glob）。
6. 词法扫描器直接改 `pos` 时行/列要重算，否则按列号切源码会切错（`(( ))` 的原文切片吃过亏）。
7. 语句列表要能把 `break`/`continue` 与 `return` 的"信号"传给外层
   （内层列表会消费标志，外层必须另看一个不会被消费的标记）。
8. 子进程要恢复 SIGINT/SIGQUIT 默认处置（shell 自己忽略，`exec` 前必须还原）。
9. 子 shell 不继承父 EXIT trap，但子进程自己设的 EXIT trap 到它退出时要跑；退出前必须 `fflush`。
10. 重定向栈要按"标记"回退（不能一律弹空整栈），否则 `while` 里第一个内建命令会把外层 `< f` 撤掉。
11. 换 fd 前后都要 `fflush`：缓冲输出在 `dup2` 之后才落盘会写错目标、甚至丢数据。
12. 追加文件时不能用 `"a"` 模式流的 `ftell` 当基准偏移（要另开一份量大小）。
13. UPX 输出文件已存在时会「Packed 0 files」静默跳过，必须先删目标。
14. 帮助文本加密时 32 字节哈希写进 `nonce[16]` → 栈溢出踩坏 key。
15. **KDF 改动必须两侧同值**：只改编译器不改运行时镜像 → 池解出乱码
    （现象：产物把乱码当命令名，报"未找到该命令"）。
16. 读口令文件不要用 `fseek/ftell`（管道上会失败）→ 改分块读到尾，`<(printf …)` 也能用。
17. **给"用户给的路径"设权限必须判 `S_ISREG`**：`s2a -o /dev/null`（丢产物只看 AST dump）时
    chmod 落到宿主 `/dev/null`，容器 `/dev` 与宿主 `/dev` 是**同一 inode** → 宿主 `/dev/null`
    被改成 0755 → Android `webview_zygote`（非 root）打不开它而 SIGABRT → 所有 WebView App
    起不来。现在全项目设权限都走 `src/common/safeperm.h` 的 `s2a_set_perm()`（非普通文件跳过，
    并打一行提示）。事故全过程：`s2a-事故记录-devnull-20260919.md`。

---

## 16. 许可证

MIT License，Copyright (c) 2026 Chen9183。

Author: **deepseek v4 flash & @Chen9183 (github)**
